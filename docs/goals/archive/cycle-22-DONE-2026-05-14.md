---
cycle-id: cycle-22-stream-d-hard-limits-permission-judge-sourcing
claim-bearing: true
status: DONE
opened: 2026-05-14
closed: 2026-05-14
supersedes: cycle-21-stream-d-hook-decomposition-adapter-hardening (DONE 2026-05-14, saturation-verified)
priority: HIGH (Stream D hard-limits / permission-judge sourcing)
governing-principle: docs/goals/README.md §"Saturation-based DONE - the harder bar"
---

# Short-Term Goal - Cycle 22: Stream D Hard-Limits / Permission-Judge Sourcing

## 1. Why this cycle

Cycle-21 completed the first behavior-preserving D-H6 hook decomposition. The remaining D-hooks item
in the master checklist is D-H7: a hard-limits / permission-judge boundary. The master plan explicitly
marks the source as pending because the prior SYNTHESIS pattern #3 was falsified and must be
re-sourced before implementation.

This cycle is sourcing-first and implementation-limited. It must establish what hard limits and
permission judgment mean in this codebase before adding runtime enforcement. If the source model is
not credible enough, this cycle closes with a documented no-go and a revised next cycle, not with
speculative policy code.

## 2. Deliverables

| # | File | Content |
|---|------|---------|
| C22-1 | `docs/excellence-application/05-architecture/stream-d-hard-limits-source-review.md` | Source review that separates evidence, inference, assumptions, and rejected options for hard-limits / permission-judge behavior. |
| C22-2 | `docs/excellence-application/05-architecture/stream-d-hard-limits-policy-sketch.md` | Narrow policy sketch for what a later deterministic hard-limit boundary may enforce, including no-go rationale for immediate runtime code. |
| C22-3 | `packages/core/src/security/hard-limits.ts` | Optional only after source review passes; no speculative enforcement before the source review is accepted. |
| C22-4 | `packages/core/test/*` | Focused tests only if code is added. If no code is added, docs/lint/index verification is the implementation proof. |
| C22-5 | `docs/goals/COMPLETE-CONSTRUCTION-GOAL.md` and `docs/INDEX.md` | Master checklist and generated docs index updated after saturation. |

## 3. DONE criteria (saturation-enforced)

This cycle reaches `status: DONE` when ALL of:

1. **Source truth is explicit** - the review states which prior pattern was falsified, what current evidence replaces it, and what remains unknown.
2. **Evidence / inference / assumption / risk are separated** - the review does not blur source facts with policy guesses.
3. **No speculative enforcement lands** - no runtime hard-limit code is added unless the source review justifies a narrow, testable module.
4. **If code lands, behavior is narrow** - any `hard-limits.ts` or permission-judge code must be pure, deterministic, tested, and not wired into unrelated gates without an explicit policy sketch.
5. **No semantic creep** - no MCP tools, adapter package behavior, prompt files, skill linting, new hook semantics, or unrelated catalog/install changes land.
6. **Truth surfaces align** - source review, policy sketch/no-go, master checklist, and current short-term goal describe exactly what landed and what remains.
7. **Verification passes** - relevant focused tests if code lands, full core tests if runtime code changes, `corepack pnpm lint`, `corepack pnpm docs:index`, and `corepack pnpm build` where applicable are green.
8. **Inventory stays truthful** - `core-file-to-context.csv` matches `packages/core/src` if source files change.
9. **Saturation check** - a critic pass tries to find source overclaim, imported false assumptions from SYNTHESIS pattern #3, premature enforcement, stale docs, and insufficient negative tests. Any material finding reopens the cycle.

```yaml
Falsifies-If:
  kill-condition: Hard-limits / permission-judge work marks done while relying on the previously falsified source, adding speculative enforcement, or failing to separate evidence from inference and assumptions.
  checkpoint-date: 2026-05-28
  evidence-anchor: docs/excellence-application/05-architecture/stream-d-hard-limits-source-review.md
  on-fail: Reopen cycle-22 as BLOCKED_HARD_LIMITS_SOURCE and complete source review before any hard-limits runtime code.
```

## 4. Books queued after this cycle

| Order | Cycle | Notes |
|-------|-------|-------|
| 23 | D-mcp tools | Resume MCP tool delivery after D-H7 is sourced or explicitly deferred. |
| 24 | Adapter package hardening | Only after D-hooks/D-MCP truth surfaces are stable. |

## 5. Status log

- **2026-05-14 20:40** - Cycle-22 instantiated after cycle-21 archived DONE. Scope is sourcing-first hard-limits / permission-judge review. Provisional risk class is M / auto mode because this controls future runtime enforcement and must avoid speculative policy.
- **2026-05-14 21:02** - Source review and policy sketch drafted. Verification ran green for `docs:index`, `lint`, full core tests (367), `build`, inventory parity (69/69/0), and post-tool dry-run `{}`. First saturation critic rejected close because the generated index and master harvest/D-H7 truth surfaces still carried stale assumptions.
- **2026-05-14 21:14** - First critic findings fixed: `docs/INDEX.md` now lists the Cycle 20/21/22 architecture docs; D-H7 is described as an open deterministic boundary, not a committed recursion/spawn/token module; HARV-12 is deferred to adapter/runtime approval instead of core `permission-judge.ts`; and `hard-limits.ts` remains intentionally absent.
- **2026-05-14 21:18** - Cycle-22 closed DONE as docs-only source review. No runtime hard-limits code landed. Final critic PASS verified the status-log fix, D-H7 open/deferred state, HARV-12 adapter/runtime deferral, Cycle 20/21/22 index entries, and absence of core `hard-limits.ts` / `permission-judge.ts`.
- **Verification** - `corepack pnpm docs:index` PASS; `corepack pnpm lint` PASS; full core PASS (32 files / 367 tests); `corepack pnpm build` PASS; inventory parity PASS (`SRC_COUNT=69`, `CSV_COUNT=69`, `DIFF_COUNT=0`); built CLI post-tool dry-run PASS (`{}`).
- **Saturation critics** - First critic REJECT found stale index/master D-H7/HARV-12 truth surfaces; second critic REJECT found stale "fixes in progress" status wording; final narrow critic PASS found no remaining blocker.

---

*This file is `claim-bearing: true` and governed by `docs/conception/05-gates-policy-spec.md` §8.4. The §3 DONE criteria + Falsifies-If block are gate-relevant assertions. Numeric checks are necessary but not sufficient; close requires the saturation critic in §3.9.*
