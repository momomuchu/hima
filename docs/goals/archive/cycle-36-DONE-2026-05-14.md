---
cycle-id: cycle-36-stream-f-benchmark-result-persistence
claim-bearing: true
status: DONE
opened: 2026-05-14
closed: 2026-05-14
supersedes: cycle-35-stream-f-benchmark-result-fixtures
next-cycle: cycle-37-stream-f-compliance-pack-schema
---

# Cycle 36 DONE - Stream F Benchmark Result Persistence

## 1. Result

Cycle 36 added schema-backed local persistence for planned and blocked benchmark result artifacts.

The new surface writes predictable `.planning/benchmarks/` files for future authorized benchmark
execution without launching SWE-bench, runtimes, model sessions, or fake executed evidence.

## 2. Delivered Artifacts

| ID | Artifact | Result |
|---|---|---|
| C36-1 | `harness benchmark write` | Writes planned/blocked benchmark result JSON under `.planning/benchmarks/swe-bench-verified/<runtime>/`. |
| C36-2 | `writeBenchmarkResultArtifact()` | Exposed helper validates through `BenchmarkResultSchema` before safe atomic write. |
| C36-3 | `packages/cli/test/index.test.ts` | Proves persisted files validate, blocked writes require a reason, executed writes are unavailable, and traversal-like instance ids fail. |
| C36-4 | `docs/excellence-application/05-architecture/stream-f-benchmark-result-persistence.md` | Documents persistence path, verification, and non-goals. |
| C36-5 | Truth surfaces | Master goal, short-term goal, archive, and docs index updated. |

## 3. Verification Evidence

| Check | Result |
|---|---|
| `corepack pnpm --filter @harness/cli test` | PASS: 79 tests. |
| `corepack pnpm exec biome check --write packages/cli/src/index.ts packages/cli/test/index.test.ts docs/conception/09-cli-commands-spec.md` | PASS after formatting one file. |
| `corepack pnpm docs:index` | PASS; `docs/INDEX.md` includes the Cycle 36 architecture note. |
| `corepack pnpm lint` | PASS. |
| `corepack pnpm build` | PASS. |
| Built CLI write+validate smoke | PASS: planned result wrote under `.planning/benchmarks/` and validated with `externalSessionsLaunched: false`. |
| Post-tool dry-run | PASS: repo-root `post-tool-use --dryRun` allowed with no policy violations. |
| Saturation sweep | PASS: only benign hits for the executed-write rejection test and explicit non-execution boundary. |

## 4. Saturation Critic

The close pass checks for:

- fake executed writes;
- hidden runtime/model launches;
- stale Cycle 36 active truth;
- benchmark result JSON outside `BenchmarkResultSchema`;
- overclaim that persistence equals benchmark execution.

No material blocker remained after the CLI tests, built-command validation, post-tool dry-run, and
stale-claim sweep.

## 5. Non-Goals Preserved

Cycle 36 did not:

- run SWE-bench Verified;
- launch real runtime/model sessions;
- collect real transcripts;
- write executed benchmark artifacts;
- claim benchmark readiness.

## 6. Next Cycle

Cycle 37 should build the local compliance-pack schema/generator surface for benchmark and runtime
evidence without claiming EU AI Act readiness or real benchmark execution.

```yaml
Falsifies-If:
  kill-condition: Benchmark persistence writes artifacts that do not validate, allows fake executed results, or launches external runtime/model sessions.
  checkpoint-date: 2026-05-28
  evidence-anchor: packages/cli/src/index.ts
  on-fail: Reopen cycle-36 as BLOCKED_BENCHMARK_PERSISTENCE and restore schema-backed local-only writes.
```
