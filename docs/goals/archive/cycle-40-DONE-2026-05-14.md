---
cycle-id: cycle-40-stream-f-cross-runtime-parity-fixture-harness
claim-bearing: true
status: DONE
opened: 2026-05-14
closed: 2026-05-14
supersedes: cycle-39-stream-f-benchmark-execution-authorization
next-cycle: cycle-41-stream-f-real-runtime-parity-authorization-boundary
---

# Cycle 40 DONE - Stream F Cross-Runtime Parity Fixture Harness

## 1. Result

Cycle 40 added a synthetic cross-runtime governance parity fixture harness.

The new schema, fixtures, and CLI validator compare the required governance fields across Claude,
Codex, and Hermes fixture JSON without probing runtimes, launching model sessions, running
SWE-bench, or claiming real runtime parity.

## 2. Delivered Artifacts

| ID | Artifact | Result |
|---|---|---|
| C40-1 | `fixtures/runtime-parity/synthetic/{claude,codex,hermes}.json` | Canonical synthetic fixture set for all three runtime targets. |
| C40-2 | `packages/core/src/schemas/runtime-parity-fixture.schema.ts` | Schema requires synthetic scope, local governance fields, and `externalSessionsLaunched: false`. |
| C40-3 | `harness runtime parity-fixture-validate` | Validates target coverage and governance-shape parity without external execution. |
| C40-4 | `packages/core/test/runtime-parity-fixture-schema.test.ts` and `packages/cli/test/index.test.ts` | Prove valid fixtures pass, missing targets fail, governance drift fails, and fake execution claims are rejected. |
| C40-5 | `docs/excellence-application/05-architecture/stream-f-cross-runtime-parity-fixture-harness.md` | Documents synthetic-only scope, drift detection, evidence, and non-goals. |
| C40-6 | Truth surfaces | Master goal, short-term goal, archive, command spec, and docs index updated. |

## 3. Verification Evidence

| Check | Result |
|---|---|
| `corepack pnpm --filter @harness/core test -- runtime-parity-fixture-schema.test.ts` | PASS: 392 tests through the package runner. |
| `corepack pnpm --filter @harness/cli test` | PASS: 99 tests. |
| `corepack pnpm exec biome check ...runtime parity touched files...` | PASS. |
| `corepack pnpm docs:index` | PASS. |
| `corepack pnpm lint` | PASS. |
| `corepack pnpm build` | PASS. |
| Built CLI fixture validation smoke | PASS: `parity: "pass"` and `externalSessionsLaunched: false`. |
| Post-tool dry-run | PASS: `post_tool` allowed with no policy violations. |
| Saturation sweep | PASS: only expected active-cycle and negative-test hits remained before closeout. |

## 4. Saturation Critic

The close pass checked for:

- hidden real runtime/model/SWE-bench launches;
- fixture-only evidence presented as production parity;
- stale Cycle 40 active truth after closeout;
- fake F3 completion wording;
- any accepted `externalSessionsLaunched: true` claim.

No material blocker remained. Final real-runtime F3 parity is still open and requires explicit
runtime/model authorization before execution.

## 5. Non-Goals Preserved

Cycle 40 did not:

- run Claude Code, Codex CLI, Hermes, SWE-bench, or model sessions;
- collect real runtime transcripts;
- authorize runtime/model spend;
- satisfy final F3 runtime parity.

## 6. Next Cycle

Cycle 41 should add a fail-closed authorization boundary for real runtime parity execution before
any real Claude/Codex/Hermes parity run can start.

```yaml
Falsifies-If:
  kill-condition: Synthetic parity fixtures are presented as real Claude/Codex/Hermes runtime parity or launch external runtime/model sessions.
  checkpoint-date: 2026-05-28
  evidence-anchor: packages/core/src/schemas/runtime-parity-fixture.schema.ts
  on-fail: Reopen cycle-40 as BLOCKED_PARITY_FIXTURE_OVERCLAIM and restore synthetic-only scope.
```
