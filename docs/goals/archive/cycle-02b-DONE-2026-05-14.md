---
cycle-id: cycle-02b-runtime-blockers
claim-bearing: true
status: DONE
opened: 2026-05-14
closed: 2026-05-14
supersedes: cycle-02-deep (DONE 2026-05-14 18:35, saturation-verified), cycle-02-analysis-discovery (DONE 2026-05-14 17:35, threshold-only), cycle-02b cancelled (CANCELLED 2026-05-14 17:50, premature)
priority: CRITICAL (RUNTIME-BLOCKER + structural protocol amendments)
governing-principle: docs/goals/README.md §"Saturation-based DONE — the harder bar"
---

# Short-Term Goal — Cycle 02b: Runtime-blockers + structural protocol amendments

## 1. Why this cycle

Cycle-02-deep (DONE 2026-05-14, archived) produced 21 ranked patterns + 3 structural protocol-failure lessons. This cycle implements the highest-leverage subset before applying any further excellence books. Two parallel work streams:

**Stream A — Runtime unblockers** (without these, hima's state machine cannot evaluate any gate):

1. **DoR/DoD 16-file governance scaffold** (SYNTHESIS pattern #1, RUNTIME-BLOCKER from S0)
2. **Hash-chained ledger** (SYNTHESIS pattern #22 — port from `gebruder/wirken` 145⭐, the cleanest open-source impl found, S7-verified code-level)
3. **Append-only events.jsonl event log** (SYNTHESIS pattern #14 — concept valid per DS2, code re-sourced from OMX `session-history.jsonl` since 12-factor-agents is prose-only)

**Stream B — Structural protocol amendments** (so the next swarm cycle does not repeat cycle-02's 3 failures):

4. **Cross-agent reconciliation step** added to `docs/goals/README.md` wave protocol (fix for the 600× star-count divergence DS1 surfaced)
5. **file:line citation must resolve to actual content** rule tightened in `docs/conception/05-gates-policy-spec.md` §8.4 (fix for the fabricated `src/safety/gate.py` citation S3 produced)
6. **Vocabulary-diversification pass** required before topical scans (fix for the DAG/evidence-gate paradigm S1-S4 missed)

## 2. Deliverables

### Stream A — Runtime (under `C:\Users\momomuchu\dev\Pipeline\hima\`)

| # | File | Content |
|---|------|---------|
| A1 | `docs/01-governance/_template-dor-dod.md` + 16 instances (`dor-01..dor-08.md` + `dod-01..dod-08.md`) | DoR/DoD per cycle. Locked-schema frontmatter per SYNTHESIS pattern #16. Each ≥3 entry/exit criteria + `Falsifies-If:` block. |
| A2 | `packages/core/src/governance/load-dor-dod.ts` + integration into `state-machine/transition.ts` | Parse 16 files; expose `loadDor(cycle)` / `loadDod(cycle)`; wire transition guards. |
| A3 | `packages/core/test/dor-dod-loading.test.ts` + `dor-dod-evaluation.test.ts` | Files parse; frontmatter validates; allow/block evaluated. |
| A4 | `packages/core/src/storage/hash-chained-ledger.ts` + tests | Port wirken design — per-event prev-hash SHA-256, Ed25519 signature per turn, SIEM-friendly JSONL line format. Wire into `planning-store.ts`. Source citation: `gebruder/wirken` (S7 code-verified). |
| A5 | `packages/core/src/storage/events-log.ts` + tests | Append-only `.hima/state/events.jsonl`. Source: OMX `session-history.jsonl` (DS2 code-verified) NOT 12-factor-agents (DS2 verified prose-only). Wire into hooks + transition. |

### Stream B — Protocol amendments

| # | File | Content |
|---|------|---------|
| B1 | edit `docs/goals/README.md` | Add new §"Wave protocol — cross-agent reconciliation". Rule: every swarm wave includes a final reconciliation step where each agent verifies the top 3 entity claims (repo names, star counts, file paths) of every other agent. Mismatches block close. |
| B2 | edit `docs/conception/05-gates-policy-spec.md` §8.4 | Tighten Falsifies-If rule: `evidence-anchor` field must resolve to actual content (file exists, line range is in the file). The gate validator does a syntactic resolve check post-write. |
| B3 | edit `docs/goals/README.md` | Add new §"Vocabulary diversification" — before launching topical research scans, enumerate ≥3 competing paradigm vocabularies (in cycle-02 case: skills/hooks vs DAG/evidence-gate vs orchestration-framework). Each paradigm gets ≥1 dedicated scan agent. |

## 3. DONE criteria (saturation-enforced)

This cycle reaches `status: DONE` when ALL of:

1. **A1 16 files written**, each non-placeholder, each Falsifies-If-compliant.
2. **A2 wired** — transition.ts calls loadDor/loadDod; no DoR-file-not-found errors in `pnpm test`.
3. **A3, A5 tests pass** — `pnpm test` in `packages/core/` green for the 4 new test files.
4. **A4 wirken-ported ledger** — `hash-chained-ledger.ts` exists with Ed25519 + SHA-256, has property-based tests, and writes to `.hima/state/ledger/<run-id>.jsonl`.
5. **B1 + B2 + B3 amendments landed** — `docs/goals/README.md` + `docs/conception/05-gates-policy-spec.md` updated; the next swarm cycle would inherit the new rules.
6. **A manual `harness transition discovery → cadrage` runs end-to-end** without "DoR file missing" or "evidence file missing" errors. State machine actually evaluates the gate.
7. **Saturation check** — at close, dispatch a 30-min DS-style critic on A1-A5 to find fabricated citations, hallucinated file paths, or missing edge cases. If critic finds any material gap, cycle re-opens.

```yaml
Falsifies-If:
  kill-condition: 2026-06-04 (= today + 21 days) passes without all of A1+A2+A3 + B1+B2 landed (A4+A5 may slip to next cycle if scope-pressed)
  checkpoint-date: 2026-05-21 (= today + 7 days, mid-cycle review — at this date, A1 template + 4-6 DoR files should exist; B1 should be drafted)
  evidence-anchor: docs/goals/SHORT-TERM-GOAL.md:66
  on-fail: shrink to A1 only (16 governance files) — that is the runtime-blocker. Defer A4, A5, B1-B3 to cycle-02c. Document the slip explicitly.
```

## 4. Saturation rule (per `docs/goals/README.md` §"Saturation-based DONE")

The numeric thresholds in §3 above are floors, not ceilings. Close requires #7 (final critic pass) finding nothing material. If the critic finds fabricated citations like S3 did in cycle-02, the cycle reopens — same rule as cycle-02-deep.

## 5. Books queued after this cycle

| Order | Cycle | Notes |
|-------|-------|-------|
| 02c | tiered-cascade routing + keyword registry | SYNTHESIS pattern #2 + #13. Requires events-log (A5) for routing observability. 4-6h. |
| 02d | SubagentStop hook as deliverables gate | SYNTHESIS pattern #15. 4-6h. |
| 03 | spec-driven-development excellence book | First excellence-book application post-runtime-unblock. 5-8h. |
| 04+ | further excellence books | Apply per cadence. Each cycle follows saturation-based DONE. |

## 6. Status log

- **2026-05-14 18:40** — Cycle-02-deep archived. Cycle-02b instantiated (refined per cycle-02-deep findings — pattern #22 wirken added to scope as A4 since it's the cleanest open-source ledger impl, pattern #14 events.jsonl re-sourced from OMX session-history.jsonl per DS2). 7 deliverables (A1-A5 runtime + B1-B3 protocol) defined. CRITICAL priority confirmed.
- **2026-05-14 21:58** — Cycle-02b closed DONE after implementation, full verification, manual transition smoke, and two critic passes. The first critic found material B2/A5 coverage gaps; those were fixed. The final critic returned no material findings.

## 7. Closing evidence — 2026-05-14

### What shipped

- A1: `docs/01-governance/_template-dor-dod.md` plus 16 DoR/DoD files under `docs/01-governance/`.
- A2/A3: `packages/core/src/governance/load-dor-dod.ts`, transition governance evaluation in `packages/core/src/state-machine/transition.ts`, and request-transition integration in `packages/core/src/services/request-transition.ts`.
- A4: `packages/core/src/storage/hash-chained-ledger.ts` with SHA-256 chaining, Ed25519 signatures, property-based tests, and writes to `.hima/state/ledger/<run-id>.jsonl`.
- A5: `packages/core/src/storage/events-log.ts`, append-only `.hima/state/events.jsonl`, and wiring through runtime events, transition, and close-run paths.
- B1/B3: `docs/goals/README.md` now includes cross-agent reconciliation and vocabulary diversification protocols.
- B2: `docs/conception/05-gates-policy-spec.md` now requires resolvable `evidence-anchor` checks, and `packages/core/src/gates/evaluate-gate.ts` enforces `MISSING_FALSIFIES_IF` for claim-bearing post-write artifacts.

### Evidence anchors

- `corepack pnpm typecheck` — PASS.
- `corepack pnpm build` — PASS.
- `corepack pnpm lint` — PASS.
- `corepack pnpm test` — PASS, 32 test files and 423 tests.
- Manual smoke: temporary project copied `docs/01-governance/`, then `node packages/cli/dist/index.js init --root <tmp>` and `node packages/cli/dist/index.js transition cadrage --subPhase Observer --root <tmp>` succeeded without DoR/DoD missing-file errors.
- Governance count audit: 16 `dor-*` / `dod-*` files, each with 3 criteria and a complete `Falsifies-If` block.
- Saturation critic: final critic pass returned "no material findings" after B2 patch-payload and malformed-block tests were added.

### Deferred

- Cycle 02c starts next: tiered-cascade routing + keyword registry (`packages/core/src/catalogs/keyword-registry.ts` and `packages/core/src/catalogs/router-cascade.ts`).

---

*This file is `claim-bearing: true` and governed by `docs/conception/05-gates-policy-spec.md` §8.4. The §3 DONE criteria + file-level Falsifies-If + saturation rule §4 are the gate-relevant assertions. This cycle does NOT close on numbers — it closes on saturation (per the README protocol amendment that landed 2026-05-14 PM).*
