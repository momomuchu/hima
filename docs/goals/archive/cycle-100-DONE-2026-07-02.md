---
cycle-id: cycle-100-close-residuals
claim-bearing: true
status: DONE
opened: 2026-07-02
closed: 2026-07-02
supersedes: cycle-99-regrounding-and-onboarding (DONE, archived docs/goals/archive/cycle-99-DONE-2026-07-02.md)
governing-principle: docs/goals/README.md §"Saturation-based DONE — the harder bar"
---

# Short-Term Goal — Cycle 100: close the cycle-99 residuals

## 1. Why this cycle exists

Cycle-99 shipped onboarding + the swappable base/pack but left three honest residuals. This cycle
closes them. All three were covered by existing specs/decisions (SPEC-016/017, ADR-0006,
HIMA-BASE-SKILLS.md) — no new spec-gate required.

## 2. Deliverable — all closed

- **R1 ✅ wire `GENERIC_DEV_CYCLE` selectable in `hima init`** via a `--generic` flag → writes
  `HimaConfig.cycle = GENERIC_DEV_CYCLE`; default (no flag) unchanged. Exported the pack symbols
  from `@hima/core`. Tests: zero-corpus resolution + default-unchanged regression.
- **R2 ✅ 15 `hima-*` `SKILL.md` bodies** at `skills/<id>/SKILL.md` (8 meta + 7 dev-cycle pack),
  each with frontmatter (name, description, stage, source: base, mode) + lean body. Coverage test
  asserts every `dev-cycle-pack.ts` id has a matching file.
- **R3 ✅ live end-to-end demo** — `e2e-full-cycle-demo.test.ts` spawns the real CLI
  (`hima init --yes` → ward bootstrap → `hima hook stage-advance` ×8) and asserts an 8-stage
  `done-verified` sealed trace + ledger archival, with no manual verify sub-agent.

## 3. DONE criteria — all met

1. ✅ R1: generic corpus-free cycle selectable + resolves zero-corpus (test).
2. ✅ R2: 15 `SKILL.md` exist; coverage test green.
3. ✅ R3: spawned-CLI demo drives init → 8 staged verdicts → sealed DoD trace.
4. ✅ `pnpm test` green (1707 / 88 files); claim-bearing guard 20/0; no gate/guard weakened; DEV_CYCLE corpus default untouched.

## 4. Residual risk (honest, deferred — not blocking DONE)

- `hima hook stage-advance` has no `--evidence` flag, so `StageVerdict.evidence` is `[]` when sealed
  via CLI; the R3 demo asserts trace-level `reason` evidence and explicitly asserts the `[]` gap.
- The CLI surface does not mechanically enforce that `verify` is sealed by an *independent* agent
  (hima-verdict's "never self-declared" rule stays a skill/process convention, not a CLI block).
- `hima-*` skills are lean bodies (~30-50 lines each); depth can grow as they're used.

## 5. Kill conditions

- Never weaken the claim-bearing guard or a live behavior gate to pass; fix the code/data.
- Tidy First: S and B commits never mixed. Non-destructive: DEV_CYCLE corpus default unchanged.

```yaml
Falsifies-If:
  kill-condition: >
    A residual was marked closed without a passing test at its criterion, OR the founder's DEV_CYCLE
    corpus default was changed, OR pnpm test / the claim-bearing guard is left red.
  checkpoint-date: 2026-07-13
  evidence-anchor: docs/specs/SPEC-PRIMITIVE.md
  on-fail: reopen cycle-100 ACTIVE; restore the failing residual's blocker; do not advance.
```

## 6. Status log

- **2026-07-02** — Cycle 100 opened + executed full-auto via workflow W3 (4 parallel author lanes:
  R1 init-wiring + 3× skill-body lanes → serial integrator: coverage test + R3 demo + green →
  adversarial verify). R1/R2/R3 all closed. **pnpm test 1707 green (88 files), guard 20/0.**
  Commits: a6a6fce (open), 9b41370 (build). **Cycle DONE** — a new short-term goal is needed
  (candidates: `--evidence` flag on stage-advance + independent-verify enforcement to close the R3
  gaps; deepen `hima-*` skill bodies; a real multi-agent session demo of criterion 1).
