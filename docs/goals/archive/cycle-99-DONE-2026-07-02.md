---
cycle-id: cycle-99-regrounding-and-onboarding
claim-bearing: true
status: DONE
opened: 2026-07-02
closed: 2026-07-02
supersedes: cycle-98-v3-build-loop (DONE, archived docs/goals/archive/cycle-98-DONE-2026-07-02.md)
governing-principle: docs/goals/README.md §"Saturation-based DONE — the harder bar"
---

# Short-Term Goal — Cycle 99: re-grounding + onboarding foundation

## 1. Why this cycle exists

After cycle-98 certified the v3 architecture, the founder re-grounded hima's vision (2026-07-02):
**not commercial; primitive = the cycle; hima is a plugin ABOVE the coding agent.** This cycle
(a) captures that re-grounding as formal specs, and (b) builds the onboarding + pluggable base/pack
registry that the re-centered vision now points to.

## 2. Deliverable

### Part A — re-grounding (DONE)
- `docs/specs/SPEC-VISION.md` (ISO/IEC/IEEE 29148 vision, claim-bearing) — rigor-reviewed.
- `docs/specs/SPEC-PRIMITIVE.md` (the cycle, traced to code SSOT) — code-alignment verified.
- `docs/goals/LONG-TERM-GOAL.md` rewritten non-commercial (derived from SPEC-VISION §1).
- `docs/decisions/0006-base-tier-agnostic-primitives.md` (OD-2 resolved: base = agnostic, dev-cycle = swappable pack).
- Private docs moved to the `private/` submodule (`momomuchu/hima-private`).

### Part B — onboarding + pluggable base/pack (DONE)
- **SPEC-016** (onboarding questions) + **SPEC-017** (apply) authored, adversarially critiqued, accepted (spec-gate honored).
- `hima init` question-flow (injectable answers + `--yes`) writes `HimaConfig` atomically, merges on
  re-run, writes `.hima/current-risk.json` + `.hima/config.example.jsonc`, reuses `runSetup` for hooks.
- Generic `hima-*` base (8 meta) + dev-cycle pack (7 stage) shipped as swappable data
  (`GENERIC_DEV_CYCLE`, corpus-free, swap-compatible with `DEV_CYCLE`).
- `enabledSources` source-filter + `useDevCyclePack` skip in resolution — both strict no-ops by default.

## 3. DONE criteria (saturation-based) — all met

1. ✅ Part A specs claim-bearing + resolving Falsifies-If; guard green (20/0).
2. ✅ SPEC-016/017 authored + accepted before any `hima init` implementation (spec-gate).
3. ✅ `hima init` writes a valid `HimaConfig`; integration + spawned-CLI e2e tests prove it.
4. ✅ `base`(8) + dev-cycle pack(7) shipped distinct + swappable; `GENERIC_DEV_CYCLE` swap-without-kernel-change test (INV-2).
5. ✅ `enabledSources` provenance filter + `useDevCyclePack` skip proven by test; `pnpm test` green (1665), guard green.

## 4. Residual risk (honest, deferred — not blocking DONE)

- The 15 `hima-*` skills are shipped as **data/manifests only** — full `SKILL.md` bodies are a later
  content task (kept lean per founder direction).
- `GENERIC_DEV_CYCLE` is shipped + swap-tested but is **not yet selectable via `hima init`** (init
  writes `useDevCyclePack`; wiring the pack as a live default is a follow-up).
- Config-file precedence stays `base<user<project`; the 4-tier `base<corpus<user<project` intent is
  satisfied at the **`SkillRef.source` provenance/filter** level (ADR-0006), not as a separate
  corpus config-file rung.

## 5. Kill conditions

- Never weaken the claim-bearing guard or a live behavior gate to pass; fix the code/data.
- Tidy First: S and B commits never mixed.

```yaml
Falsifies-If:
  kill-condition: >
    hima init was implemented before SPEC-016/017 were accepted (spec-gate bypass), OR the base tier
    ships with the dev-cycle pack fused in (non-swappable, violating ADR-0006 / SPEC-PRIMITIVE INV-2),
    OR pnpm test / the claim-bearing guard is left red.
  checkpoint-date: 2026-07-13
  evidence-anchor: docs/specs/SPEC-PRIMITIVE.md
  on-fail: reopen cycle-99 ACTIVE; restore the failing part's blocker; do not advance the loop.
```

## 6. Status log

- **2026-07-02** — Cycle 99 opened (supersedes cycle-98 DONE). **Part A ✅** (3 review lanes:
  code-alignment clean, 29148 rigor applied, consistency reconciled; private submodule pushed).
  **Part B ✅** — specs via W1 (author+critique+converge, found F1 corpus gap); build via W2 (2 parallel
  author lanes + integrator to green + adversarial verify). `hima init` + config filter/skip + swappable
  `hima-*` pack, TDD. **pnpm test 1665 green, guard 20/0.** Commits: 450384f, 54ad7ca, c05b701, 30c9337,
  13fbe6a, ef86129. **Cycle DONE** — a new short-term goal is needed (candidates: full `hima-*` SKILL.md
  bodies; wire `GENERIC_DEV_CYCLE` selectable in `hima init`; live end-to-end DoD demo per LONG-TERM crit 1).
