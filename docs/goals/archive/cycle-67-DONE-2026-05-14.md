---
cycle-id: cycle-67-hard-limits-boundary-selection
claim-bearing: true
status: DONE
opened: 2026-05-14
closed: 2026-05-14
supersedes: cycle-66-completion-audit-runtime-boundary
next-cycle: cycle-68-api-stability-review
---

# Cycle 67 DONE - Hard-Limits Boundary Selection

## 1. Result

Cycle 67 selected and implemented one deterministic D-H7 hard-limit boundary:

`packages/core/src/security/hard-limits.ts`

The selected boundary blocks remote script pipe execution in normalized PreToolUse shell payloads.
It does not complete broad D-H7 hard-limits coverage.

## 2. Delivered Artifacts

| ID | Artifact | Result |
|---|---|---|
| C67-1 | Boundary note | `stream-d-hard-limits-boundary-implementation.md` selects blocked command patterns. |
| C67-2 | Negative tests | `handle-hook.test.ts` RED test failed before implementation for remote-script pipe execution. |
| C67-3 | Implementation | `evaluateHardLimits()` blocks `curl/wget | sh/bash` and PowerShell `Invoke-* | Invoke-Expression`. |
| C67-4 | Verification | Focused tests, touched-file check, docs index, lint, post-tool dry-run, and saturation passed. |
| C67-5 | Truth surfaces | Master goal, short-term goal, archive, architecture note, and docs index updated. |

## 3. Verification Evidence

| Check | Result |
|---|---|
| RED focused test | PASS: `handle-hook.test.ts` failed before implementation on expected `BLOCKED_COMMAND_PATTERN`. |
| GREEN focused test | PASS: 425 tests through the package runner. |
| `corepack pnpm exec biome check ...hard-limits touched files...` | PASS. |
| `corepack pnpm docs:index` | PASS. |
| `corepack pnpm lint` | PASS. |
| Post-tool dry-run | PASS: `post_tool` allowed with no policy violations. |
| Saturation sweep | PASS: only active-cycle text, source-review warnings, and explicit non-goals remained. |

## 4. Saturation Critic

The close pass checked for:

- broad hard-limits overclaims;
- D-H7 full-completion overclaims;
- token-budget, subagent inheritance, or spawn-limit implementation claims;
- runtime adapter proof overclaims;
- LLM permission-judge-in-core drift.

No material blocker remained. Token budgets, subagent tool inheritance, spawn limits, and runtime
adapter proof remain open.

## 5. Non-Goals Preserved

Cycle 67 did not:

- implement token-budget ceilings;
- implement subagent disallowed-tool inheritance;
- implement spawn-count limits;
- call an LLM or permission judge;
- prove runtime adapter enforcement beyond normalized PreToolUse payloads.

## 6. Next Cycle

H2 API stability review is local-safe and should run before release/publication work.

```yaml
Falsifies-If:
  kill-condition: Hard-limits documentation claims broad D-H7 completion, token-budget enforcement, subagent tool inheritance, or runtime adapter enforcement beyond the tested PreToolUse command pattern.
  checkpoint-date: 2026-05-28
  evidence-anchor: docs/excellence-application/05-architecture/stream-d-hard-limits-boundary-implementation.md
  on-fail: Reopen cycle-67 as BLOCKED_HARD_LIMITS_OVERCLAIM and restore one-boundary scope.
```
