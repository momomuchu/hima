---
cycle-id: cycle-91-harv18-anti-bypass-local-detection
claim-bearing: true
status: DONE
opened: 2026-05-15
closed: 2026-05-15
supersedes: cycle-90-h3-install-matrix-execution (BLOCKED 2026-05-15)
next-cycle: cycle-92-harv16-preference-router-local
---

# Cycle 91 DONE - HARV-18 Anti-Bypass Local Detection

## 1. Result

Cycle 91 advanced HARV-18 from a repo-local skill fixture to local core bypass-attempt detection.

The implementation centralizes anti-bypass patterns in `packages/core/src/security/anti-bypass-clause.ts`
and wires them through the existing gate evaluator for prompt, tool, post-tool, and subagent-start
inputs. It still does not close HARV-18 end to end because live runtime permission enforcement,
adapter hook firing, real bypass-attempt transcripts, and real `~/.hima` installation remain missing.

## 2. Delivered Artifacts

| ID | Artifact | Result |
|---|---|---|
| C91-1 | Anti-bypass helper | `packages/core/src/security/anti-bypass-clause.ts` defines the HARV-18 finding catalog and evidence anchor. |
| C91-2 | Gate integration | `packages/core/src/gates/evaluate-gate.ts` uses the helper for `user_prompt`, `pre_tool`, `post_tool`, and `subagent_start`. |
| C91-3 | Tests | Direct helper, gate-level, and normalized hook-service tests cover prompt bypass, hook-wiring mutation, and subagent bypass instructions. |
| C91-4 | Evidence note | `docs/goals/harv18-anti-bypass-local.md` records local proof and non-goals. |
| C91-5 | Truth refresh | Master goal, runtime boundary, completion audit, stream note, archive, and short-term goal updated without closing full HARV-18 or H3. |

## 3. Verification Evidence

| Check | Result |
|---|---|
| `corepack pnpm --filter @harness/core test -- anti-bypass-clause.test.ts gates.test.ts handle-hook.test.ts` | PASS: 46 files / 471 tests. |
| `corepack pnpm exec biome check ...touched code/test files...` | PASS after formatting. |
| `corepack pnpm docs:index` | PASS. |
| `corepack pnpm lint` | PASS. |
| `corepack pnpm build` | PASS. |
| Post-tool dry-run | PASS: hook post-tool-use allowed with no policy violations. |
| Saturation sweep | PASS: full HARV-18 row remains unchecked; runtime permission, adapter hook, real bypass transcript, and user-home blockers remain present. |

## 4. Saturation Critic

The close pass checked for:

- HARV-18 end-to-end completion overclaims;
- runtime permission enforcement overclaims;
- real adapter hook wiring proof overclaims;
- real `~/.hima` install overclaims;
- stale H3 closure after Cycle 90 blocker;
- missing bypass tests for normalized hook payloads.

No material blocker remained for the local scope. Full HARV-18 remains open.

```yaml
Falsifies-If:
  kill-condition: Cycle 91 claims HARV-18 complete beyond local core detection, or removes the runtime permission, adapter hook, real bypass transcript, or real user-home blockers.
  checkpoint-date: 2026-05-29
  evidence-anchor: docs/goals/archive/cycle-91-DONE-2026-05-15.md
  on-fail: Reopen cycle-91 as BLOCKED_HARV18_OVERCLAIM and restore the local-only boundary.
```
