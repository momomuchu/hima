---
cycle-id: cycle-84-harv11-subagent-tool-deny-local
claim-bearing: true
status: DONE
opened: 2026-05-15
closed: 2026-05-15
supersedes: cycle-83-harv09-prompt-injection-scanner-local
next-cycle: cycle-85-harv02-skill-linting-local
---

# Cycle 84 DONE - HARV-11 Subagent Tool-Deny Local Proof

## 1. Result

Cycle 84 implemented local HARV-11 SubagentStart default-deny behavior for subagent tool requests.

The evidence artifact is:

`docs/goals/harv11-subagent-tool-deny-local.md`

The construction ledger remains 119/155 checklist rows complete, 76.8 percent. HARV-11 remains
open as a full row until live subagent runtime execution, adapter permission projection, and real
user-home install or invocation evidence exist.

## 2. Delivered Artifacts

| ID | Artifact | Result |
|---|---|---|
| C84-1 | `packages/core/src/hooks/hook-payload.ts` | Normalizes SubagentStart tool policy fields. |
| C84-2 | `packages/core/src/gates/evaluate-gate.ts` | Blocks default-denied and inherited-denied tool requests with `SUBAGENT_TOOL_DENIED`. |
| C84-3 | `packages/core/test/gates.test.ts`, `packages/core/test/handle-hook.test.ts` | Local gate and hook-normalization proof. |
| C84-4 | Truth surfaces | Master goal and Stream G notes mention local tool-deny proof while keeping live runtime and real install blockers open. |
| C84-5 | Next cycle | `docs/goals/SHORT-TERM-GOAL.md` advances to HARV-02 local skill-linting work. |

## 3. Verification Evidence

| Check | Result |
|---|---|
| Focused core tests | PASS: 440/440 through `corepack pnpm --filter @harness/core test -- gates.test.ts handle-hook.test.ts`. |
| `corepack pnpm docs:index` | PASS. |
| `corepack pnpm lint` | PASS. |
| `corepack pnpm build` | PASS. |
| Post-tool dry-run | PASS: `post_tool` allowed with no policy violations. |
| Saturation sweep | PASS: no live subagent runtime execution, adapter permission projection, real user-home install, beta, H3 OS, benchmark, publication, payment, legal, market, or SIEM completion claim remained. |

## 4. Saturation Critic

The close pass checked for:

- HARV-11 full row incorrectly marked complete;
- local gate blocking represented as live runtime enforcement;
- adapter-level permission projection claims;
- real `~/.hima` install claims;
- stale Cycle 84 ACTIVE state.

No material blocker remained for the local tool-deny cycle.

## 5. Non-Goals Preserved

Cycle 84 did not:

- write to real `~/.hima`;
- invoke a live subagent runtime or model;
- prove adapter-level permission inheritance;
- close HARV-11 end to end;
- run H3 CI, benchmark, beta, publication, payment, legal, market, or SIEM lanes.

## 6. Next Cycle

Cycle 85 should implement repo-local HARV-02 skill-linting proof without CI publication,
external runtime execution, or real user-home writes.

```yaml
Falsifies-If:
  kill-condition: Cycle 84 closure is treated as proof that HARV-11 completed end to end with live subagent runtime enforcement, adapter permission projection, or real user-home install proof.
  checkpoint-date: 2026-05-29
  evidence-anchor: docs/goals/harv11-subagent-tool-deny-local.md
  on-fail: Reopen cycle-84 as BLOCKED_HARV11_PROXY_COMPLETION and restore the live-runtime/user-home blockers.
```
