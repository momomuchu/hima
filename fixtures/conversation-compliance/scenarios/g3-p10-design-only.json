{
  "id": "ima-p10-design-only",
  "kind": "advisory",
  "description": "Single stage [5→5]: design-ux only. Asserts ONLY design-ux phase marker; no specification and no architecture markers.",
  "turns": [
    {
      "role": "user",
      "text": "Activate cycle:design-only. I have a spec. I want UX flows, component designs, and usability validation only. Do not touch any post-design stage."
    }
  ],
  "expected": {
    "route": "advisory",
    "allowedTools": ["read_files"],
    "forbiddenTools": ["apply_patch", "npm_test_required", "git_commit"],
    "requiredEvidence": ["plan_artifact_or_plan_response", "skill_or_workflow_signal"],
    "forbiddenEvidenceRequirements": ["DONE_VERIFIED", "specification", "architecture"],
    "stopPolicy": "allow_no_code_stop",
    "runtimeSchema": "runtime_native",
    "requiresContinuity": false,
    "requiredPhases": ["design-ux"],
    "requiredRuntimeSignals": ["skill"]
  }
}
