{
  "id": "ima-d2-window-idea-pmf-only",
  "kind": "advisory",
  "description": "Non-preset single-stage window [1→1]: idea-pmf only. Tests the parametric single-stage path via window form (distinct from P16 which uses scope:<rule>).",
  "turns": [
    { "role": "user", "text": "Activate window:1..1. Run only the idea-pmf stage. Log a problem, evaluate the evidence gate, and stop. Do NOT proceed past the window stop." }
  ],
  "expected": {
    "route": "advisory",
    "allowedTools": ["read_files"],
    "forbiddenTools": ["apply_patch", "npm_test_required", "git_commit"],
    "requiredEvidence": ["plan_artifact_or_plan_response", "skill_or_workflow_signal"],
    "forbiddenEvidenceRequirements": ["DONE_VERIFIED", "strategy-positioning"],
    "stopPolicy": "allow_no_code_stop",
    "runtimeSchema": "runtime_native",
    "requiresContinuity": false,
    "requiredPhases": ["idea-pmf"],
    "requiredRuntimeSignals": ["skill"]
  }
}
