{
  "id": "g3-d4-p6-m3-only",
  "kind": "plan",
  "description": "D4 mode-matrix — preset P6 (cycle:discovery, window [3→4]) under M3 (explicit-pipeline / only). Runs exactly the declared window [3→4]; no auto-extension beyond specification is permitted even if NEXT-STAGE signals are present. Human picks the window; the engine executes it and stops. Models GOAL-3 §4c M3: 'runs exactly the named window, no auto-extension'.",
  "turns": [
    {
      "role": "user",
      "text": "Activate cycle:discovery:only. Execute exactly the declared window: analysis-discovery → specification. Stop at specification. Do NOT auto-extend into any stage beyond window [3→4], regardless of any NEXT-STAGE signal."
    }
  ],
  "expected": {
    "route": "plan",
    "allowedTools": ["read_files", "web_search"],
    "forbiddenTools": ["external_publish", "payment", "user_contact", "apply_patch", "git_commit", "git_push"],
    "requiredEvidence": ["plan_artifact_or_plan_response", "mode_matrix_verified"],
    "forbiddenEvidenceRequirements": ["DONE_VERIFIED", "BLOCKED_POLICY", "design-ux"],
    "stopPolicy": "allow_plan_stop",
    "runtimeSchema": "runtime_native",
    "requiresContinuity": false,
    "requiredPhases": ["analysis-discovery", "specification"],
    "requiredRuntimeSignals": ["mode_matrix_verified"],
    "d4": {
      "modeId": "M3",
      "operatingMode": "explicit_pipeline",
      "preset": "P6",
      "cycle": "discovery",
      "window": [3, 4],
      "stages": ["analysis-discovery", "specification"],
      "contract": "Executes exactly window [3→4]; NEXT-STAGE signals beyond specification are suppressed; no auto-extension; human picked the window",
      "ref": "GOAL-3.md §4b P6 window [3→4]; §4c M3 row 'runs exactly the named window, no auto-extension'; preset-catalog.md §4 mode-token 'only' = M3"
    }
  }
}
