{
  "name": "hima",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "node scripts/with-build-lock.mjs --shell \"corepack pnpm --filter @norm/schemas build && corepack pnpm --filter @norm/storage-core build && corepack pnpm --filter @norm/core build && corepack pnpm --filter @norm/cli build\"",
    "typecheck": "node scripts/with-build-lock.mjs --shell \"corepack pnpm --filter @norm/schemas typecheck && corepack pnpm --filter @norm/storage-core typecheck && corepack pnpm --filter @norm/core typecheck && corepack pnpm --filter @norm/cli typecheck\"",
    "test": "node scripts/with-build-lock.mjs -- node scripts/run-tests.mjs",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@types/node": "^24.10.2",
    "fast-check": "^4.3.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
